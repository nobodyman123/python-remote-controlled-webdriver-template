from selenium import webdriver
import remote_buttons as rb
import scenes

name = ""

def load_scene(driver):
    response = None

    #do stuff i.e. find certain elements

    return response

def input_handler(driver, buttonPressed):
    newScene = None
    response = None

    #if buttonPressed = some_button: do_some_stuff()

    return (newScene, response)